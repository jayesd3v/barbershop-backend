{
    "compilerOptions": {
        "module": "Preserve",
        "moduleResolution": "bundler",
        "baseUrl": "./",
        "paths": {
            "@/*": ["./src/*"] // this help vscode to jump to definition when import using @/
        }
    }
}
